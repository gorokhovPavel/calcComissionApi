import {ITransaction} from '../models'

/**
 * Calculating input info of all transactions
 * @param transactionList list of information about each transaction
 */
export const getCalculatedComission = (transactionList: ITransaction[]) => {
    console.log(transactionList)
}
